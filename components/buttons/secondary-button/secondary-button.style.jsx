import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    secondaryButton: {      
        minHeight: 40,
        width: 180,
        paddingVertical: 10,
        flexDirection: 'row',
        justifyContent: 'center',
    },
    buttonText: {
        color: 'white',
        fontWeight: 'bold',
        fontSize: 20,
    }
});