class Experience {
    constructor(id, theme, title, text) {
        this.id = id;
        this.theme = theme;
        this.title = title;
        this.text = text;
    }
}

export default Experience;