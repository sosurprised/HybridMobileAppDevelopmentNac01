import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    button: {
        marginTop: 30,
        flexDirection: 'row',
        justifyContent: 'flex-end',
        marginHorizontal: 5,
    }
});